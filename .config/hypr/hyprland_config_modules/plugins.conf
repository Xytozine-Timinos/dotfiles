exec = ~/.config/hypr/scripts/hyprland_plugins.sh

plugin {
    hyprbars {
	bar_blur = true
        bar_height = 38
        bar_color = rgb(16161d)
        col.text = rgb(e0e6ed)
        bar_text_size = 17
        bar_text_font = JetBrainsMono Nerd Font
	bar_text_weight = bold
	bar_text_align = left
	bar_buttons_alignment = right
        bar_button_padding = 12
        bar_padding = 15
        bar_precedence_over_border = true
	inactive_button_color = rgb(26282f)
	on_double_click = hyprctl dispatch fullscreen && notify-send "Fullscreen!" "Use Mod+F to undo it." -t 2100
        hyprbars-button = rgb(ff6f91), 22, , ~/.config/hypr/scripts/kill_window.sh, rgb(16161d)
        hyprbars-button = rgb(f9e2af), 22, 󱂬, hyprctl dispatch togglefloating && hyprctl dispatch resizeactive exact 70% 70%, rgb(16161d)
        # hyprbars-button = rgb(a6e3a1), 22, , pkill -USR1 waybar
	hyprbars-button = rgb(a6e3a1), 22, , ~/.config/hypr/scripts/hide_unhide_window.sh h, rgb(16161d)
	hyprbars-button = rgb(81ccee), 22, 󰖲, ~/.config/hypr/rofi_hyprland/rofi-window-ws-switch/workspace_input_number_window_switch.sh, rgb(16161d)
   }
}

plugin {
	touch_gestures {
		# The default sensitivity is probably too low on tablet screens,
		# I recommend turning it up to 4.0
		sensitivity = 4

		# must be >= 3
		workspace_swipe_fingers = 3

		# switching workspaces by swiping from an edge, this is separate from workspace_swipe_fingers
		# and can be used at the same time
		# possible values: l, r, u, or d
		# to disable it set it to anything else
		workspace_swipe_edge = du

		# in milliseconds
		long_press_delay = 150

		# resize windows by long-pressing on window borders and gaps.
		# If general:resize_on_border is enabled, general:extend_border_grab_area is
		# used for floating windows
		resize_on_border_long_press = true

		# in pixels, the distance from the edge that is considered an edge
		edge_margin = 10

		# emulates touchpad swipes when swiping in a direction that does not trigger
		# workspace swipe. ONLY triggers when finger count is equal to 
		# workspace_swipe_fingers.
		#
		# might be removed in the future in favor of event hooks
		emulate_touchpad_swipe = false
	}
}

plugin {
    overview {
        # --- Behavior & Layout ---
        onBottom = false
        exitKey = Escape
        preserveAspect = true
        affectStrut = false
        showNewWorkspace = false
        disableGestures = true       # Gestures disabled as requested

        # --- Rendering & Visibility ---
        # Note: drawActiveWorkspace = false fixes the hover transparency bug
        drawActiveWorkspace = false
        disableBlur = false
        dragAlpha = 1.0

        # --- Layer Management ---
        # These ensure standard UI elements don't disappear in overview
        hideRealLayers = false
        hideOverlayLayers = false
        hideTopLayers = false
        hideBackgroundLayers = false

        # --- Colors & Styling ---
        # Focused Workspace
        workspaceActiveBorder = rgba(b4befeff)
        workspaceActiveBackground = rgba(b4befeff)

        # Unfocused Workspace (Using your preferred dark color)
        workspaceInactiveBorder = rgba(16161dff)
    }
}
